query getProduct($slug: ID!) {
  product(id: $slug, idType: SLUG) {
    ... on VariableProduct {
      slug
      name
      regularPrice
      salePrice
      image {
        sourceUrl(size: WOOCOMMERCE_SINGLE)
      }
    }
  }
}
